# @open-knowledge-stack/okf

Installs the native `okf` executable for the current operating system and architecture. The package contains no OKF application logic; platform binaries are delivered as optional dependencies.

```bash
npm install --global @open-knowledge-stack/okf
okf --help
```
